# 划线独立复习状态设计

## 背景

MarkBuddy 目前把“是否加入复习”建模为一种高亮标签。只要某条划线的 `tags` 数组包含配置里的复习标签，默认是 `学习`，它就会进入复习队列。这个模型让复习状态看起来像普通分类标签，也导致侧边栏里的复习入口容易被误解成列表条目默认带了一个“复习”标签。

现在的产品方向已经明确：

- 复习用于记住具体划线摘录，而不是复习整篇网页。
- 保存划线时不应自动加入复习。
- 用户必须手动把某条摘录加入复习。
- 复习状态应独立于普通标签。
- 旧的复习标签不迁移，也不再被识别为复习状态。

## 目标

- 保持复习对象为单条划线。
- 对用户可见文案使用“唤醒收藏 / 重温 / 提醒我再看”，降低考试式记忆负担。
- 使用独立的 `review.enabled` 字段保存复习开关。
- 将 SM-2 排期状态保存到 `review.sm2`。
- 保留现有网页收藏和普通标签行为。
- 从实际复习模型中移除“复习标签”配置。
- 让侧边栏里的按钮明确表现为“提醒我再看”的动作，而不是标签。
- 在扩展图标和网页浮动入口展示当前待重温数量。

## 非目标

- 不做网页级复习队列。
- 不让新划线自动加入复习。
- 不迁移旧的 `tags: ['学习']` 数据。
- 不提供旧复习标签继续进入复习队列的兼容模式。
- 不修改现有 SM-2 评分算法本身。

## 数据模型

新划线默认保持非复习状态：

```js
{
  id,
  url,
  text,
  color,
  savedAt,
  serializedRange,
  active: true
}
```

当用户手动把某条划线加入复习后，该高亮新增：

```js
review: {
  enabled: true,
  sm2: {
    interval: 0,
    easeFactor: 2.5,
    repetitions: 0,
    nextReviewAt: 0
  }
}
```

用户提交复习评分后，用现有 SM-2 计算结果替换 `review.sm2`。用户把划线移出复习时，设置 `review.enabled = false`，不删除普通标签，也不影响网页收藏。

SM-2 的 `interval` 单位为天。实现中统一用一天的毫秒数计算 `nextReviewAt`，避免测试期间按分钟排期的临时代码进入正式逻辑。

## 行为

待复习查询只包含启用了独立复习状态的有效划线：

```js
highlight.active !== false &&
highlight.review?.enabled === true
```

一条划线在以下情况视为到期：

- 已启用复习，但没有 `review.sm2`；
- 或 `review.sm2.nextReviewAt <= Date.now()`。

只有旧复习标签的历史划线不会进入待复习队列。这是有意设计：新模型从明确的独立复习状态开始，不隐式继承标签语义。

## 界面

在每条划线行内：

- 未加入提醒时，显示“提醒我再看”按钮，并提供同义无障碍文本。
- 已加入提醒时，显示“不再提醒”。
- 点击 `+` 会启用 `review.enabled`。
- 点击“不再提醒”会关闭 `review.enabled`。

设置面板不应再暴露“复习标签”作为有效配置。如果保留说明文案，应描述“手动选择划线摘录进行定时提醒”，而不是描述某个可配置标签。

今日重温提示条和回顾模式保留当前的位置和交互方式，但数据来源改为新的待重温查询。三档反馈文案为“暂时没用 / 再看看 / 仍然有用”，仍映射到原有 SM-2 评分。

待重温数量显示在两个位置：

- Chrome 扩展图标徽标，由后台脚本通过 `chrome.action.setBadgeText()` 更新；
- 网页浮动入口徽标，由后台脚本广播 `REVIEW_BADGE_UPDATED` 给内容脚本更新。

后台使用 `chrome.alarms` 每小时刷新一次徽标，并在切换提醒、提交回顾反馈、删除划线、删除收藏或保存设置后立即刷新。

## 存储消息

后台脚本应使用复习专用操作替代基于标签的复习更新，例如：

- `UPDATE_HIGHLIGHT_REVIEW`，参数为 `{ id, enabled }`；
- `GET_DUE_REVIEWS`；
- `UPDATE_REVIEW_RESULT`。

`UPDATE_HIGHLIGHT_TAGS` 只保留给普通标签编辑使用。如果划线标签后续没有普通编辑入口，可以在实现阶段再评估是否清理。

## 备份与导出

JSON 备份应保留新的 `review` 对象，因为备份本来就包含完整高亮记录。Markdown 导出暂时不需要展示复习状态，除非后续单独设计导出复习信息的功能。

## 测试

需要覆盖以下重点：

- 新划线默认不启用复习；
- 只有旧 `tags: ['学习']` 的划线不会进入待复习队列；
- 手动启用复习后，有效划线会立即进入待复习队列；
- 提交复习评分后会更新 `review.sm2`；
- SM-2 `interval` 按天计算，首次记住后应排到约 1 天后；
- 关闭复习后，该划线不再进入待复习队列；
- 侧边栏复习按钮使用独立复习状态，而不是标签。
- `manifest.json` 包含 `alarms` 权限；
- 待重温徽标会随后台刷新广播到内容脚本。

## 未决事项

无。已确定采用“划线级别、手动加入、独立复习状态、不迁移旧标签”的模型。
