# 开发任务清单（Tasks）

> 按阶段跟踪构建进度。状态：`[x]` 已完成 · `[/]` 进行中 · `[ ]` 待完成

---

## Phase 1 — 基础架构

- [x] `manifest.json` — Manifest V3 配置，声明权限、入口、内容脚本
- [x] 图标资源生成 — `icons/icon-16.png`, `icon-48.png`, `icon-128.png`
- [x] `service-worker.js` — 后台框架，注册右键菜单，设置侧边面板行为
- [x] `content/content.css` — 高亮样式、浮动工具栏样式、Toast 样式

## Phase 2 — 核心功能

- [x] `content/content.js` — 浮动工具栏渲染与定位
- [x] XPath 选区序列化 / 反序列化
- [x] 页面加载时批量恢复高亮（`requestAnimationFrame` 分批处理）
- [x] 收藏网页（`__markbuddy_savePage`）
- [x] 保存划线（`__markbuddy_saveHighlight`，自动关联 bookmark）
- [x] 删除高亮（悬停按钮 + DOM 恢复）
- [x] Toast 通知（操作结果反馈）

## Phase 3 — 侧边面板

- [x] `side-panel/panel.html` — 结构：Header / 设置面板 / 搜索 / 标签栏 / 列表 / 弹窗
- [x] `side-panel/panel.css` — CSS 变量主题系统，`prefers-color-scheme` 深/浅色适配
- [x] `side-panel/panel.js` — 数据加载与列表渲染
- [x] 收藏卡片：favicon + 标题 + 网址 + 时间 + 标签 + 操作按钮
- [x] 划线列表：二级展开/折叠，色点 + 原文展示
- [x] 实时搜索（关键词高亮）
- [x] 标签过滤（多选 OR 逻辑）
- [x] 设置面板：预设颜色选择 + 自定义颜色输入

## Phase 4 — 完善与边界处理

- [x] 删除网页收藏（级联删除关联划线）
- [x] 删除单条划线（面板内操作）
- [x] 标签管理（添加标签 Modal，支持选择已有标签）
- [x] 颜色设置持久化（`chrome.storage.local` → settings key）
- [x] 所有 async 操作添加 try/catch 错误处理
- [x] XPath 失效时降级跳过，不崩溃
- [x] 划线附加备注/笔记功能（已实现宿主页气泡框编辑与侧边栏双向同步）

## Phase 5 — 学习探索与高级交互（已完成）

- [x] **商店权限精简**：从 `manifest.json` 移除 `<all_urls>` 宽泛权限，保护隐私并缩短商店审核周期
- [x] **SPA 页面路由自愈高亮**：监听 `chrome.webNavigation.onHistoryStateUpdated`，多层延时自动恢复高亮
- [x] **多重列表排序**：支持最新/最早收藏、最近修改时间、域名 A-Z/Z-A 等排序，并支持本地持久化
- [x] **艾宾浩斯复习系统**：SM-2 遗忘曲线算法、今日待复习横幅、全屏卡片式学习环境、三挡评分及复习完成总结面板
- [x] **抽屉滑出式设置面板**：重构设置面板为 position: fixed 从右侧平滑滑出，带粘性顶部及返回箭头
- [x] **自绘确认弹框**：使用原生 `<dialog>` 配合红色警告 Icon、按钮缩放动效自定义删除确认框
- [x] **动态标签与独立删除**：标签栏依据过滤后数据动态重算，卡片标签集成 inline `×` 独立移除功能
- [x] **复习标签 Hover 提示框**：复习标签新增 SVG 信息图标，悬停展现高保真 SM-2 算法解析 Tooltip

---

## 接下来应该完成的工作（按优先级排序）

### 1. P0 最高优先级：商店发布准备 & 关键兼容性修复
- [ ] **发布素材与隐私政策准备**：
  - [ ] 制作两张 1280x800 像素的展示截图（Screenshot 1 & 2）。
  - [ ] 制作 440x280 像素的推荐图（Promo Tile）。
  - [ ] 部署公开的隐私政策（Privacy Policy）页面（可使用 GitHub Pages 或 Gist 托管静态 Markdown 文件）。

### 2. P1 高优先级：核心体验提升
- [ ] **历史高亮位置纠错与自愈**：在页面 DOM 发生微调导致偏移量定位轻微失效时，基于文本内容相似度匹配进行降级恢复，并自动更新数据库中的偏移位置。

### 3. P2 中优先级：数据安全与快捷交互
- [ ] **数据导入与导出**：在设置面板添加一键「导出备份（JSON）」和「导入恢复」，实现本地数据可迁移，防止浏览器缓存清除导致的数据意外丢失。
- [ ] **全局键盘快捷键**：配置 Chrome Shortcuts 命令行，支持通过快捷键（如 `Cmd+Shift+L` 或 `Ctrl+Shift+L`）快速唤起侧边栏，或直接对当前选中文本进行默认颜色的高亮保存。

### 4. P3 低优先级：容量监控与未来规划
- [ ] **存储空间展示与清理**：在设置中显示 `chrome.storage.local` 的已用空间百分比（上限 10MB 监控），并提供「一键清空全部数据」功能。
- [ ] **云端自动同步**：基于 Chrome Sync 存储或自建云存储 API，支持跨浏览器多端高亮同步。
